<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team10.mapper.UserMapper">
    <sql id="Base_Column_List">
        id,
        username,
        `password`,
        account_no
    </sql>
    <resultMap id="User" type="com.team10.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="account_no" property="account_no"/>
    </resultMap>
    <select id="detail" resultMap="User">
        select * from user
        <include refid="UserFindCriteria"></include>
        limit 1
    </select>
    <insert id="create"  keyProperty="id" useGeneratedKeys="true" parameterType="com.wcx.entity.User">
        insert into user(id,username,password,account_no)
            value(#{id},#{username},#{password},#{account_no})
    </insert>
    <sql id="UserFindCriteria">
        <where>
            <if test="id !=null">and id = #{id}</if>
            <if test="username !=null and username !=''" >and username like concat('%',#{username},'%')</if>
            <if test="password !=null and password !=''" >and password = #{password}</if>
            <if test="account_no !=null and account_no !=''">and account_no = #{account_no}</if>
        </where>
    </sql>
</mapper>

